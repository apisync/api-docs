custom&#8209;fields <span class="attribute-required">requerido</span><span class="attribute-explanation">object</span> | Objeto contendo valores que descrevem o item. As chaves e valores são livres. Todas as chaves serão convertidas para formato com hífen automaticamente. Ex.: `my_key` se torna `my-key`.
