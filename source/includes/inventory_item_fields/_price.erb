price
<span class="attribute-explanation">object, optional</span> |
Object with these keys:
<ul>
  <li>`amount`: price in cents, e.g. `1012` for `R$10,12`. It can't be `0` (zero). to avoid settings a price, just leave it `null`.</li>
  <li>`currency`, e.g. `BRL`, `USD`.</li>
</ul>
