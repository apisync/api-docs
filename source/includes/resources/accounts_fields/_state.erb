state
<span class="attribute-explanation">string</span> |
Defines the account state, whether it's active or not. Defaults to `active`.
<br /><br />
Accepted values:
<ul>
  <li>`active`: this means the account will be processed and synchronized
  with other services (e.g Facebook, Google).</li>
  <li>`inactive`: entity is removed and becomes invisible.</li>
</ul>
Set an account as inactive to stop incurring costs.
