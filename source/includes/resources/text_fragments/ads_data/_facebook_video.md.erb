**video**

```
{
  "data": {
    "page-id": "12731923769",
    "thumbnail-url": "https://example.com/image.jpg",
    "video-url": "http://example.com/video.avi"
  }
}
```

These are the keys you have to provide.

- `page-id` (string, optional): when you leave this blank, you have to specify a `title`.
- `thumbnail-url` (string)
- `title` (string, optional): when you leave this blank, you have to specify a `page-id`.
- `video-url` (string)
- `instagram-permalink-url` (optional): Instagram post URL.

<%= image_tag "ads/facebook_video.png" %>
