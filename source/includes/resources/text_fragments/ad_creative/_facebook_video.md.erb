**video**

```
{
  "data": {
    "story": {
      "video_data": {
        "link": "https://example.com",
        "message": "The main body of the post, above the image.",
        "image_url": "https://example.com/ad-picture.png",
        "video_url": "http://example.com/video.avi",
        "link_description": "Link description below the video.',
        "call_to_action": {
          "type": "OPEN_LINK",
          "value": {
            "link_caption": "example.com/subscribe"
          }
        }
      }
    }
  }
}
```

These are the keys you have to provide.

- `story.link_data.link` (string): the URL the user will be redirected to when
clicking the ad.
- `story.link_data.message` (string): the main body of the post, above the image.
- `story.link_data.image_url` (string): URL of the ad thumbnail used when the
video is not playing.
- `story.link_data.video_url` (string): URL of the video to be uploaded to
Facebook.
- `story.link_data.link_description` (string): link description below the video.
- `story.link_data.call_to_action.type` (string): one of the values defined
in [this page](https://developers.facebook.com/docs/marketing-api/reference/ad-creative-link-data-call-to-action/). Use `OPEN_LINK` for generic ads that take users
to other pages, `LIKE_PAGE` for page likes.
- `story.link_data.call_to_action.value.link_caption` (string): This is the
URL that will show up to the user in the ad link.
This has to be similar to `story.link_data.link`.

<%= image_tag "ads/facebook_video.png" %>
