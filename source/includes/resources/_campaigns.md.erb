# Campaigns <span>beta</span>

A campaign defines how ads are going to be run. This is synchronized with
3rd-party services like Facebook Ads and Google Ads.

**Daily Budget:** Facebook requires a daily budget per ad set, whereas
Google, on the other hand, is not as granular and requires it on the entire
campaign. To see how to define a budget,
see the <a href="#service-configurations">Configurations</a>
section for details.

Below are the descriptions of the attributes for this resource.

```json
{
  "data": {
    "id": "49c23f61-2fec-5f1c-86dd-6afe36a1aef7",
    "type": "ads",
    "attributes": {
<%= partial("includes/resources/campaigns_examples/json_response_attributes.erb").rstrip %>
<%= partial("includes/resources/generic_examples/json_response_service_configurations_empty.erb").rstrip %>
    },
    "relationships": {
<%= partial("includes/resources/campaigns_examples/json_response_relationships.erb").rstrip %>
    }
  }
}
```

<% table = [] %>
<!-- required -->
<% table << partial("includes/resources/campaigns_fields/service.erb") %>
<% table << partial("includes/resources/generic_fields/state.erb") %>
<!-- optional -->
<% table << partial("includes/resources/campaigns_fields/description.erb") %>
<% table << partial("includes/resources/generic_fields/reference_id.erb") %>
<% table << partial("includes/resources/generic_fields/service_configurations.erb") %>
<% table << partial("includes/resources/generic_fields/title.erb") %>
<!-- readonly -->
<% table << partial("includes/resources/generic_fields/created_via.erb") %>
<% table << partial("includes/resources/generic_fields/created_at.erb") %>
Attributes | &nbsp;
------------------- | -----------
<%= table.map { |r| r.gsub(/\n/, " ") }.join("\n") %>

<% rels = [] %>
<% rels << partial("includes/resources/campaigns_rels/ad_account.erb") %>
Relationships | &nbsp;
------------------- | -----------
<%= rels.map { |r| r.gsub(/\n/, " ") }.join("\n") %>

## Creating campaigns

> Definition <br> `POST /campaigns`

Use this endpoint to create new campaigns.

<% table = [] %>
<!-- required -->
<% table << partial("includes/resources/campaigns_fields/service.erb") %>
<% table << partial("includes/resources/generic_fields/state.erb") %>

<!-- optional -->
<% table << partial("includes/resources/campaigns_fields/description.erb") %>
<% table << partial("includes/resources/generic_fields/reference_id.erb") %>
<% table << partial("includes/resources/generic_fields/service_configurations.erb") %>
<% table << partial("includes/resources/generic_fields/title.erb") %>

Attributes | &nbsp;
---------- | -----------
<%= table.map { |r| r.gsub(/\n/, " ") }.join("\n") %>

<%= raw_partial("includes/resources/text_fragments/_success_or_error.md") %>

**Important:**

<%= raw_partial("includes/resources/text_fragments/_note_creation_requires_rels.md") %>
<%= raw_partial("includes/_reference_id_note_post.md") %>

## Updating campaigns

`PUT /campaigns/:id`

> Definition <br> `PUT /campaigns/:id`

<%= raw_partial("includes/resources/text_fragments/_put_intro.md") %>

<% table = [] %>

<!-- required -->
<% table << partial("includes/resources/generic_fields/state.erb") %>

<!-- optional -->
<% table << partial("includes/resources/campaigns_fields/description.erb") %>
<% table << partial("includes/resources/generic_fields/reference_id.erb") %>
<% table << partial("includes/resources/generic_fields/service_configurations.erb") %>
<% table << partial("includes/resources/generic_fields/title.erb") %>

Attributes | &nbsp;
---------- | -----------
<%= table.map { |r| r.gsub(/\n/, " ") }.join("\n") %>

**Important:**

<%= raw_partial("includes/resources/text_fragments/_note_cannot_update_rels.md") %>
<%= raw_partial("includes/_reference_id_note_put.md") %>

<%= raw_partial("includes/resources/text_fragments/_success_or_error.md") %>

## Reading campaigns

<%= raw_partial(
  "includes/resources/text_fragments/_get_standard.md",
  resource_url_fragment: 'campaigns'
) %>

<%= raw_partial(
  "includes/resources/text_fragments/_get_id_standard.md",
  resource_url_fragment: 'campaigns'
) %>

<%= raw_partial(
  "includes/resources/text_fragments/_get_filter_intro.md",
  resource_url_fragment: 'ad-accounts/:ad-account-id/campaigns'
) %>

Filters | &nbsp;
------- | -----------
<% table = [] %>
<% table << partial("includes/resources/campaigns_fields/filter_service.erb") %>
<%= table.map { |r| r.gsub(/\n/, " ") }.join("\n") %>

## Deleting campaigns

<%= raw_partial("includes/resources/text_fragments/_delete_via_state.md") %>
